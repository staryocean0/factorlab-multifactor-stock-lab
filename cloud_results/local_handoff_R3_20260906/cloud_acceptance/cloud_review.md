# R3 零拟合对照：云端验收与证据用途更新

任务：**LCL-R3-NOFIT-20260906-01**。被复核本地提交：`d340f249f01d3e413d876cf9096d789114fc6070`。冻结脚本发布提交：`d6804cdb2b321f7b5ae74eb98fc8dcc5f59613d6`。日期：2026-09-06。

## 1. 验收裁决

**本任务按声明范围验收完成，状态为 `completed_with_limits`，不要求本地重做本轮比较。** 现任 K1 在同一组已消费 2017 review 样本上，相比四个预先固定、零拟合的历史 epsilon 基准，平均横截面 RankIC 更高；在本次最强的四基准之一 `negative_mean10_epsilon` 上，配对差分别为 **+0.0267050563 / +0.0271326483**。

这将“相对这四个简单基准的描述性增量尚未建立”更新为“在本次固定样本与口径上已观察到”。**不更新**条件特征独立贡献、独立样本外、全量 PIT、总收益或扣费账户收益的结论；不授予模型晋级、训练或交易权限，不据此增加 K 或开启潜残差网络。

保留[本地反馈](../local_feedback.md)、[原比较报告](../comparison.json)和[逐日结果](../paired_daily.csv)原文及字节。本验收是后继裁决，不覆盖原研发、R2 或首次 R3 回执。

## 2. 实际验收范围与计算地点

本地已记录：原 FactorLab `b39bb12f43a46b165d18db93191a669234077444`；冻结脚本 SHA-256 `188d9a54746d95e079770cef701837d827880c61bc542dd2ad6f9a4729367e9f`；实际命令退出 0；13 项本地测试通过；无训练、checkpoint 重推理、分钟读取或账户重放。

云端本轮实际完成：

1. 读取本地反馈、比较报告、逐日 CSV 的全部 490 条文本记录、当前协议/状态及冻结比较实现。逐日文件来自 Git blob `9e2cfe3fb3711e35d872d2f08c875ef88f0ffcd8`，报告来自 `3be56c5483229b9cced7813b2e671384b112dddc`；这是连接器返回的身份定位，不称为云端对原 CSV 原字节重哈希。
2. 原交付包中脚本和测试的物理字节分别重算为 Git blob `2bb0f6dd326091d8aa35cc7ce9b73b5244e70f01`、`314bdbf0f9c05975aede8b22f020a716f593b471`，与本地提交所含同路径身份一致；使用这份真实源码在当前云端运行 **13 项测试，零失败、零错误、零跳过**。
3. 从连接器读取的逐日记录中保留 K1 和 `negative_mean10_epsilon` 的日期、支持、RankIC、十分位差及 Top30 相对全支持均值，形成 **98 条配对投影**。两时钟所有49日均保留，未按结果挑选日期。用独立聚合程序重算关键均值、相位、胜负日和支持，**57 项一致性检查全部通过**。该投影不是490行原CSV的原字节副本；其他三基准接受为冻结脚本产生的本地结果，其汇总差值做算术核对，本轮未在云端逐行重新计算它们。

云端不持有本轮使用的原 epsilon、原 review 分数和坐标数组；**没有重新计算任何个股级 IC、原分数或原数组摘要**。接收的是“本地计算、云端源码/报告复核，并重算关键逐日聚合”的分层证据，而不是云端独立全量复验。完整数字与执行说明见 [cloud_checks.json](cloud_checks.json)；57项检查可用本目录程序与投影复算，原完整检查列表和JUnit同时保存在本次下载证据包。

## 3. 比较结果

目标保持为 `epsilon_future_raw_h20_financial_residual_not_total_stock_return`。两个时钟各覆盖 **82,321 个日期×股票坐标、49 个D5决策日**，日期为2017-01-06至2017-12-29；每日至少1447、至多1894只股票。49日不是49个互不重叠的H20结果。

### 全部预定基准（本地结果，未按结果翻转方向）

| 预测器 | 14:30 平均 RankIC | 14:45 平均 RankIC |
|---|---:|---:|
| K1 原三种子逐日 rank-z 集成 | 0.1599258704 | 0.1615257472 |
| 最近一期 epsilon 延续 | 0.0501942581 | 0.0490975236 |
| 最近一期 epsilon 反转 | -0.0501942581 | -0.0490975236 |
| 十个原历史端点均值延续 | -0.1332208142 | -0.1343930989 |
| 十个原历史端点均值反转 | 0.1332208142 | 0.1343930989 |

“均值反转最强”只是对四个已全部报告基准的事后描述，不是把它追封成事前唯一主检验，也不是选择一个有利方向后丢弃其他结果。这里的十个端点按原输入相隔20交易日，不是最近连续10日均值。

### 与均值反转的关键配对（云端聚合复算）

| 指标 | 14:30 | 14:45 |
|---|---:|---:|
| 平均 RankIC 差 K1−基准 | +0.0267050563 | +0.0271326483 |
| RankIC 差中位数 | +0.0191278401 | +0.0232699579 |
| RankIC 优于基准的决策日 | 36/49 | 33/49 |
| 十分位组差的平均增量 | +1.3074233307 个百分点 | +1.3891642236 个百分点 |
| 十分位组差优于基准的决策日 | 38/49 | 39/49 |
| Top30 相对全支持均值的平均增量 | +0.6228615238 个百分点 | +0.4710429131 个百分点 |
| 上述 Top30 指标优于基准的决策日 | 26/49 | 25/49 |

组差及 Top30 数字都是**H20金融残差的截面统计**，不是原始股票收益、可交易多空收益、净值增长或年化收益。相同支持下，Top30相对全支持均值的配对差也等于两个Top30组的残差均值之差；不代表账户执行了这些组。

K1均值和十分位差复原原 formal 结果：14:30 RankIC差约2.8e-17，14:45为0，十分位差均为0；原冻结容差仍为1e-4。云端聚合一致性使用1e-12，仅用于同一份小表数字的核对，不另加金融显著性阈值。

四个相位的平均RankIC增量均为正，但逐日仍有负增量。事后按日历季度描述，第二季度增量为 -0.0089565640 / -0.0013296451；这只说明全年优势不等于所有时段占优，**不据此挑选季度、做择时开关或改变参数**。两时钟逐日增量相关约0.978864，不作为两个独立市场实验。未计算iid标准误、p值、独立性假设下的胜率显著性或净收益。

## 4. 证据解释的变化与不变

可以新增的表述：**现任冻结K1在既有2017金融残差评价中，不仅有正相关，也观察到超过这四个固定简单历史规则的平均排序表现。** 这足以保留其作为进一步比较的候选，不构成全部模型家族最优或普适预测有效的证明。

仍不能新增：

- 条件特征已经产生独立增量。K1与历史规则同时相差模型表示、训练、非线性结构及集成等，不能把全部差值归因于状态/行业/规模条件。
- 约0.027就是条件贡献，或简单基准与K1的IC比例就是“解释比例”。相关指标不具有这种可加归因含义。
- 第二轮上一自然月CloudRidge条件已经有效。它不等于现任已有OT状态/暴露输入，本任务也没有比较它。
- 已解决历史选型使用2009—2020结果、事件可得性、成交时钟或独立OOS问题。`fresh_oos=false`及R2限定不变。
- Top30均值较高就可以宣告主动多头扣费盈利。逐日胜率和收益对象已经不同，尚无对应账户结论。

本任务无必须返工的执行/汇总错误；不为增强结论而要求重扫同一批原数组、补全六模型推理或重新拟合。原冻结脚本与13项测试保持不变。本轮57项是小证据聚合一致性检查，不能与13项软件测试相加后称作70项市场验证。

## 5. 收口与下一动作

同一沟通记录更新为**云端已复核，`completed_with_limits`**。R3状态为 `nofit_baseline_comparison_reviewed_consumed_only`。清除“本地待执行”和“相对这四基准尚无结果”的过时动作；R2不重开，旧Stage4与账户来源局限不改变。

**下一动作仍由云端负责：设计能区分“收益历史建模增益”和“原有条件特征增益”的最小可比对照，先查可复用的已存在结果。** 对照须同目标、同支持、同选型/训练信息范围和明确训练预算；单纯推理时置零或打乱特征只能作为依赖敏感性诊断，不能等同于合理训练的无特征模型。新月度条件单独定义，不混入本轮结论。

此次验收不创建新的本地数据任务，不自动运行训练、容量搜索、潜残差网络、账户或Actions。若下一研究确需新本地计算，再按现行Protocol写最小任务；不以全部历史未知清零为前置，也不扩成又一轮基础设施审计。

## 6. 复算本轮小证据

在本目录执行，输出文件必须是新文件：

```bash
python review_projection.py --root . --output /新目录/recheck.json
```

这只重新聚合已披露的98行投影与报告摘录，不访问网络或原大数组。原文件仍定位在同一任务上一级目录。代码、投影、参考数值、检查结果和测试日志均保存；不把未发生的检查填成通过。
