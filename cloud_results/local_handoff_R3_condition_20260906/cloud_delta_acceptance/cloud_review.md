# R3 F/H 无训练增量：云端验收与任务收口

日期：2026-09-06。任务：`LCL-R3-COND-20260906-01`。
被复核提交：`360eae62df47583d185df00c2955dfe0723facb9`；六次 H 拟合原交付为 `e17aacb4ce933ad397b5d5d541e7269a2237c226`。

## 1. 裁决

**本轮任务 completed_with_limits，原无训练补充已完成，不再要求同一批补件或新增拟合。** 接收逐 seed 的保存分数评价、集成重载一致性、当前源码/normalizer 身份说明，以及 H 权重未持久化的更正。CPU/CUDA、原 F 运行时源码/normalizer 未完全绑定、H 权重不可载入是保留的证据限制，不再伪装成等待补齐即可获得完整证明的任务。

当前接收的是已消费 2017 材料上的真实运行方案的描述性比较。**没有认证“唯一变化是 X”的严格效应，没有认证独立 OOS、全量 PIT、经济因果或扣费账户 alpha。** 同一任务完成，不等于这些更强主张也通过。

依据：[本地增量](../local_feedback_delta.md)、[逐 seed 汇总](../per_seed_metrics.json)、[逐 seed 日表](../per_seed_daily.csv)、[来源与持久化](../source_persistence.json)。原反馈和原数组不改写，先前 R2/NOFIT 验收不重开。

## 2. 逐 seed 结果，以及不能混用的结论

两时钟各 82,321 个评价坐标、49 个决策日，2017-01-06 至 2017-12-29。下表是本地从保存分数计算、经云端汇总与关键日期投影复核的 H20 金融残差 RankIC，不是账户收益。

| 时钟 | seed | F 均值 RankIC | H 均值 RankIC | F−H |
|---|---:|---:|---:|---:|
| 14:30 | 11 | 0.155524 | 0.141293 | +0.014231 |
| 14:30 | 29 | 0.156984 | 0.136844 | +0.020139 |
| 14:30 | 47 | 0.148857 | 0.148614 | +0.000244 |
| 14:45 | 11 | 0.155828 | 0.126901 | +0.028927 |
| 14:45 | 29 | 0.161647 | 0.142494 | +0.019153 |
| 14:45 | 47 | 0.151227 | 0.145247 | +0.005980 |

六个全年均值差均为正，但大小相差很大。14:30 seed47 几乎无全年平均增量；其相位2均值差为 −0.007793，14:45 seed47 的相位2也略负（−0.000119）。不能把“集成四相位均正”改写为“每个 seed 的四相位均正”。云端对 seed47 两时钟全部日期重聚合后，RankIC 胜出日分别为29/49与34/49；这是针对弱增量 seed 的定向检查，不是随机抽样，不据此评估全部 seed 的稳定性。

集成结果保持原值：F 的均值 RankIC 为0.1599258704/0.1615257472，H 为0.1440533686/0.1405261862；F−H 为+0.0158725018/+0.0209995610。本地用保存的 float32 分数重新做逐日 rank-z 集成，报与原结果差为0；云端核对其摘要与前次已接收集成结果一致，**未重读本地分数数组来独立重建集成**。

H 的集成均值仍高于此前四个固定简单基准，但并非每个 H seed 都如此，例如14:45 seed11的0.126901低于该时钟均值反转基准0.134393。种子共享市场与样本，不能当作六份独立市场验证。

“大部分优势由历史表示学习解释”的原句已撤回，撤回正确。F−H、H−B和它们的比例不是唯一可加的金融机制归因。本次没有新增显著性检验、择优 seed、季度筛选或参数选择。

## 3. 来源和权重：已明确事实，不追加恢复仪式

**源码身份。** 三个上层文件 fit-prefix/training/preflight 的本地记录仍对应原定位。追加的 paper、Stage6 engine、初始化模块只给出当前 FactorLab 快照身份，并明确不能证明是历史 F 训练时字节。本轮接收身份说明，不把三个上层摘要相同升级为所有数值路径相同；也不把字节差异本身当成实际计算错误。严格数值等价仍未认证。

**标准化。** 两时钟重算 normalizer 摘要与 H-run wiring 记录一致，fit_end_year=2016、样本65536、未使用 target。原 F formal 未保存运行时 normalizer 摘要，因此不据这次重算补签历史 F 的运行时证书。给出的有序 review 索引摘要是索引身份，不单独证明全部原数组内容、股票名序与标签身份。

**H 权重。** 本地更正为未落盘。原 runner 保存 NPY 分数后删除内存模型，run02没有 checkpoint。现有分数及其评价可以复用；不能从这次 H 权重开展新推理，也不能说权重已验收或原模型可加载。以后必要的新训练是新实验身份，不能为补记录而训练并冒充原权重。此次不要求新训。

**后端。** F 历史 CUDA、H 本次 CPU 的差异没有隔离。它不是结果错误的证明，也不是可忽略影响的证明。与未闭合的历史数值身份一起，限制“纯 X 效应”的结论。

## 4. 云端修复及实际执行

本地新增 `finite_gaps_pass` 只在测试文件中运行，没有接入实际 `wiring_check`。云端完成了后继修复：在活动 runner 中增加 `validate_wiring_receipt`，保留原损失/潜向量/预测差的不同容差，在设置 `passed` 前拒绝 NaN、Infinity、缺字段、无效负差和非有限 forecast。

本次仅改变 `wiring_check` 并增加该检查函数。AST 对照确认 `fit_h_seed`、模型构造、分数比较、主入口等另外11个函数不变。原执行源码在原 Git 提交及原回执摘要 `4c3d3826…` 保留；后继源码摘要为 `6d33f810…`。**修复不倒填原运行记录、不改变模型或原结果，也不授权重新运行训练命令。**

实际执行范围：

| 检查 | 本次结果 | 边界 |
|---|---|---|
| 输入视图33项 + 冻结评价13项 + 后继判定39项 | 85 passed，0失败/错误/跳过 | 新39项含实际检查函数和真实wiring函数体的合成协作测试，不是完整FactorLab集成 |
| 本地提交的runner测试6项 | 3 passed，3因本地根目录缺失skip | 原文件blob核对一致；跳过未算通过 |
| 小证据检查 | 59项通过，0失败 | 六个汇总格的差值/相位、集成重载记录、seed47两时钟全部日期的RankIC投影 |

逐日日表本地交付588行。云端本轮独立重聚合的是 seed47 两时钟的196条 F/H 记录，形成98个配对投影；其他四组 seed 的详细指标保留为本地计算，并核对汇总算术。投影为连接器文本选列，不是原CSV物理字节副本。没有重算个股级 RankIC、原数组哈希、真实模型、训练或推理；没有运行全仓测试或 Actions。

结果见 [cloud_checks.json](cloud_checks.json)，59项明细可用 `python recheck_delta.py` 从随附小输入复算。源码修复测试为 `tests/unit/test_reaka_r3_wiring_validation.py`。此前测试结果不与本次叠加为独立市场证据。

## 5. 收口与下一动作

沟通任务改为 `completed_with_limits`，R3当前为 `condition_comparison_review_complete_descriptive_only`；同任务补件、拟合、重新推理均不再要求。本地目前无新待办。不是把未知问题刷绿，而是将“材料已交、计算已复核”和“强预测主张未建立”分开。

下一步由云端先整理真正能够提高证据强度的后继评价设计：因果选型与评价时段隔离、可比数值实现和运行环境、模型/标准化在运行时持久化。不继续围绕已消费2017调参，不自动增加E/K/残差网络，不自动开放新数据或交易账户；若新设计确需本地计算，再单独给出最小任务。

旧Stage4、账户来源、事件可得性等限制保留，但不再成为无关工作的前置门。此次不合并main，不改变现任模型和本地生产指针。
