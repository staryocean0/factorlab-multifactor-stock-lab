# TRANSFER-INPUT 增量独立复核与用途限定

复核日期：2026-09-08。任务：`LCL-R3-TRANSFER-INPUT-20260907-01`。
本地边界反馈：`3ea55e503296761233f992b97ca4ae7decbaf349`；本地执行基点：`53c9acf50e2cfd2efcd8a73fa74e1eaddb0ff6a1`。
原输入构建：`1c9f78f7f31282473f715ce7302be867d1aea440`。

## 裁决与并行交付

**沿已发布的最终验收，将原任务收口为 completed_with_limits；允许复用当前冻结特征仓作已消费历史上的后继算法比较，不要求重复B1—B3、本任务成员补件或重建大数组。**

保存前发现开发分支新增两次并行提交，HEAD为`04b56cde7aaf1070d2448e812ce7dbfa455c95f1`，新增`cloud_final_acceptance/cloud_review.md`和`cloud_checks.json`。这两次提交没有新增成员来源材料。本报告不覆盖它们，补上当前会话的独立计算及更精确的证据用途边界。

**接收当前产物不等于认证全部上游生成口径或历史来源。**B2成员内容指纹及生成规则对应在已读反馈中仍未建立；这一缺口保留为限制，不改写成已证明，也不继续以同一补件循环阻断所有云端接口开发。后继结果只能表述为“旧冻结模型在本次重建特征仓上的回顾性跨期算法比较”，不能据此声称严格同定义来源完整认证、fresh OOS或PIT。

当前特征仓依旧无未来标签；本次没有运行24项评分或增加模型训练。

## 1. 云端实际验证

本地delta相对53c9acf仅新增边界检查脚本、JSON、反馈和测试日志，并更新沟通状态。原生产桥接、构建器及原manifest未在该delta中变更。

云端从前次挂载包恢复代码，按GitHub当前源码恢复后继桥接及合同测试。桥接Git blob严格匹配`65470dfb783026bfa70faac1cd53609a4b866f12`；合同测试匹配`80c463e6ae59b149905099e348d25f9cdcd1cfb6`。实际复跑三个原模块：**71 passed，0 failures/errors/skipped**。

完整236行`boundary_checks.json`还原后匹配Git blob `11c8855666b4704bace6b7f1796b5e4ee4e959f9`。在该小回执上执行**163项结构、计数、误差与声明核对**，全部通过。另编写独立小回执校验器，**25项反例测试通过**，覆盖缺项、空比较、NaN/Infinity、shape/支持异常、伪造exact、过大接缝误差及错误成功状态。两组测试互不重复，但不是96项市场实验。

未执行本地原数组重哈希/重建、真实OT或状态生成器、模型加载/训练/推理、逐股RankIC、新期标签评价、全仓测试。大数组计算接收为本地执行证据，不能称云端全量独立复验。

## 2. B1：保留原状态数组

两时钟各6×1212×14=101,808个尾部基底格全部有限，各factor与variant的非有限数均为0。因此恢复原有限行口径不会删除本次实际观测。全局day_position从3406接续的修正保留；实际消费者按trading_day排序的依据沿原生产器复核与本地说明接收。

针对这一适配器差异，**没有发现需要重建state_values/state_available的实际影响**。这不是本轮重新生成并逐格比对全部真实状态，也不是整个新尾部所有来源问题均已证明不存在。

## 3. B3：旧末端D5接缝受限通过

比较点为2020-12-31、日历位置3405，每时钟重新计算1个股票暴露刷新点，未以复制旧数组冒充新生成答案。

| 对象（每时钟） | 格数/有限支持 | 最大绝对差 |
|---|---|---|
| beta | 55,748，双方全有限 | 0 |
| reliability | 55,748，双方全有限 | 0 |
| available | 55,748，双方全有限 | 0 |
| epsilon | 3,982格；双方3,662有限，支持相同 | 0 |
| 因子基底 | 84格，双方全有限 | 14:30为6.938893903907228e-17；14:45为9.020562075079397e-17 |

`seam_all_exact=false`原样保留；基底不是逐位相等。其差异约为float64舍入量级，而已检下游float32暴露、reliability及边界epsilon精确一致，本轮不据此要求重建。独立校验器的1e-12绝对上界只是明确披露的事后数值诊断，不是事前模型效力/绩效阈值，也不补签整个计算链等价。

此结论限于所检查的旧末端D5，不是整个2021—2025逐日核验。计算范围为新增2个股票暴露刷新点；基底投影日范围每时钟4,498，不能将范围乘法写成精确lstsq调用次数。此前486仅为原股票暴露刷新决策点。

## 4. B2：接收时序统计，保留来源不确定性

| 来源 | 读入行 | asof<2026 | asof排除 | 原3982映射后保留 | 映射排除 | 缺日期/逆向生效 |
|---|---:|---:|---:|---:|---:|---:|
| CloudRidge | 2,168,383 | 2,024,789 | 143,594 | 1,790,492 | 234,297 | 0/0 |
| core（原目标过滤后） | 1,308,602 | 1,256,522 | 52,080 | 1,217,859 | 38,663 | 0/0 |

数量守恒；提交的保留集合上effective_date>asof_date无违例。core的“读入行”已先做目标过滤，不是整个parquet行数；缺日期0也不能外推到此前被日期或symbol规则排除的原始行。

CloudRidge有asof=2025-12-31、effective=2026-01-05的记录。对于截至2025的数值时间线，该点不会成为2025已生效成员；但这不是源文件不含2026或首次可读时间的证明。

### 物理读取没有事后变成完全隔离

原桥接先全文件读取后过滤。本次delta的membership_audit和两个时钟load_used_membership也有混合年份文件读取路径。因此不能将`did_not_reread_2026_membership_as_a_new_scientific_pass=true`当作未读取2026内容的I/O证据。用途声明与物理读取事实分开记录，不再要求通过额外重读撤销已经发生的事实。

没有看到本轮读取2026未来收益标签或训练网络的代码证据；读取范围问题不自动等于已证明未来收益泄漏。

### 尚未建立的更强主张

本次反馈没有给出实际有界成员快照的内容摘要，也没有完整说明condensation集合的上游生成规则与原L1/SMALL/LARGE口径对应。路径、行数、14个target_id和日期顺序不能替代这些内容；2020单日接缝一致也不能单独证明后续五年规则完全一致。

因此本轮只能接收当前冻结特征仓及已检查边界。**不声明成员来源生成规则已完全认证，不声明历史首次可读时间已验证。**这些不足仍留在后继报告限制中；没有足够根据要求立即重算全部输入，也不在原任务里继续索取同一份大表。未来若要提出更强来源/PIT主张，再针对该主张补足直接证据，不能将它偷偷附加为已消费历史算法比较的全局前置审批。

## 5. 不依赖原“成功”字段的复核

本地run()的`boundary_delta_complete_no_array_rewrite`没有纳入B3误差；seam_all_exact生成式还会跳过缺少exact的比较项，正常结束统一返回0。因此exit0只表示执行完成，不能作自动验收章。

新增verify_boundary_evidence.py逐项要求五类接缝存在、shape/有限支持有效、误差有限且符合所披露数值规则。缺项、空比较、较大差异均有反例。本次真实小回执比较项齐全，下游误差为0，故该检查程序缺口本身不要求重新执行真实数据计算。原报告和脚本不倒填修改。

## 6. 下一步与不再派发的任务

原TRANSFER-INPUT任务收口；当前无新的本地补件、训练或推理任务。保留原数组、manifest、选型、normalizer和checkpoint，不新增消融组，不恢复未知CloudRidge公式，不读取2026补标签。

下一步由云端接通独立label/evaluation sidecar与fresh-process冻结模型评分接口。四组原模型、两个时钟、三个seed、最多24个新期间评分任务的已登记边界不变；**本次没有执行这些评分，也不把工程输入验收升级成预测有效性验收。**未来评价必须继续披露成员来源限制、原3982固定队列、2018—2020常量state-mask解释，以及consumed/fresh_oos=false身份。

复算命令：

```bash
python cloud_results/local_handoff_R3_transfer_inputs_20260907/cloud_delta_acceptance/verify_boundary_evidence.py \
  --input cloud_results/local_handoff_R3_transfer_inputs_20260907/boundary_checks.json \
  --output /new/path/cloud_checks.json
```
