# V1.4 接手复核与本地盘点补充

## 裁决

本轮接手审计期间，检测到同一 PR 分支已有并行完成的 V1.4（`14fc345003258102866990263e1528dc39835142`）。复核其 CURRENT 路由、任务工作流、白皮书、状态、检查器及 CI 后，保留这套实现，不覆盖、不叠加第二套准入/全仓冻结体系。基线的审计和验证回执原样保留。

当前架构把稳定数学语义、临时研究状态、运行身份与历史证据分离。R2 只限制依赖其上游来源的复现和主张，不阻断无关文档、数学预分析、接口实现或合成反例。普通维护用 Git 和针对性测试，不要求逐次重封全仓、十二轮年度会话、固定 K 顺序或整湖上传。

## 此次实际补充

提交 `3b7286efa043d1b69cb6bd91002be46a764ded3c` 增加 `scripts/reaka_local_source_receipt.py` 和 23 项反例测试，并接入现行 manifest 的组件定位与工作流第 4 节。

它把“数据所在地执行、只返小回执”落实为可运行的第一步：只读取恢复清单中的有界路径，输出相对路径、大小、摘要及未绑定/缺失/错误，不搬运数组，不递归追整湖，不运行旧训练或账户脚本。请求重复键/非有限值、路径越界、读取中源文件变化均不静默放行。摘要口径未知时不选择有利候选冒充匹配；旧账户来源缺口继续保留。

这不是完整 R2 检验器。此次只在合成文件上测试盘点器，未对真实本地 P6.1/P6.2 执行盘点或 PIT 重建。没有新增训练准入系统、全局权限开关或第二套接手索引。

## 实际集成验证

[Actions 34013765793](https://github.com/staryocean0/factorlab-multifactor-stock-lab/actions/runs/34013765793) 在真实合并快照 `ece51c470b13579f708e8ef3577ca2d00148aefc` 执行，合并 main 输入为 `af2e478aaff5c8ef7f753424b57fd2d19019f248`。

**409 项测试通过，零失败、零错误、零跳过；42 项现行入口/组件定位与语义检查通过，当前链接错误为零。** 23 个已发现单元模块中实际执行 21 个，两个历史模块因缺 `cloudridge_3_0_hybrid_filter_bank.py` 明确列为未执行，未计入通过。没有改动该排除范围来迎合此次结果；新增盘点测试自动纳入。

导航扫描覆盖该快照物化的 84 份 Markdown、2625 个链接目标。2491 项历史导航限制仍披露；它们不等于本有界仓库必须恢复整个原项目。扫描不证明所有旧文字和外链正确。

机器摘要见 [local_receipt_validation.json](local_receipt_validation.json)。本次已读取真实 job 日志及 GitHub artifact 元数据；未独立下载 ZIP 重算，不冒充此前基线回执所记录的 ZIP 独立验收。原 JUnit、完整测试范围、环境和导航清单保存在该次 Actions 的 artifact `9983281133`。

## 后续接手

现行入口仍为根 `CURRENT.json`。默认读取 AGENTS、V1.4 工作流、research_state；改数学才按需读白皮书和相关反例，复现旧结果才追原来源。不回到旧对话的“全局等待补齐”，也不因本次基础设施通过声称 R2、全量测试或策略效力已通过。

下一实际研究动作是按具体金融断言定位已有源码的时钟与收益构造，设计小型决定性反例；需要真实数据时在其所在环境执行有界检查。不要求再次泛化重构基础设施，不重新索取已验收且身份未变的 R1 文件。

本回执提交只新增证据说明，不改变已经测试的代码、工作流或现行指导。PR 是否合并以实际状态为准；本次没有修改 main、原始数据、旧回执、模型、账户结果或用户本地 FactorLab 指针。
