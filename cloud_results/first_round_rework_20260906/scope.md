# 第一轮补件验收与必要返工（2026-09-06）

用户已通知六项输入、旧合同和拟合/checkpoint 来源上传，并授权验收后继续必要返工。本次固定数据提交 `2575f9e4d12289fa3916d14f27cb637908614fd9`，沿用 V1.3 数学语义，只执行只读核验；不改写 V1.3 来源闭包、旧合同或旧回执。

工作流在独立输入 checkout 上逐块检查 `.parts`，将完整原字节恢复到临时目录，再对照先前执行回执。检查两时钟的输入数组、六份 checkpoint 的所有张量和 state digest、normalizer、拟合选择记录、冻结分数及市场坐标，以及 2023/2024 日线的文件摘要与分区日期。

源码被当作历史证据读取，不执行旧入口。历史路径仅能通过显式映射及相同摘要解析到归档副本。缺失或漂移如实保留，不重封旧摘要。

验收分为：交付字节一致、内部结构/身份一致、上游 PIT 来源可验证。前两项通过不等于最后一项成立，也不等于金融效力已重验。当前 fit-prefix 实现用历史 epsilon 序列做训练目标；`epsilon_future` 用于 review 标签，不能把它描述为直接监督该拟合的未来标签。r0 仍只关闭潜动力残差修正。

后续只针对已证实影响的环节返工；若上游残差构造、状态/暴露拟合或原始交易时钟缺件，停在 R2 等待。冻结分数不因其他 K/残差分支缺陷而自动重算。报告退出码 2 表示来源证据阻断，非策略失败；0/1/2 不是金融收益验收。

复现命令见 `.github/workflows/r1-upload-audit.yml`。报告和运行回执完成后追加于本目录。数据只读，2018—2025 仍为 consumed comparison，`fresh_oos=false`；没有训练、选 K、新分数、账户重放或生产动作。
